{
  "name": "heirloom-platform",
  "private": true,
  "version": "0.0.0",
  "description": "Heirloom platform monorepo — TS DSL + engine + REST server + CLI. Spec: https://github.com/0xnicholas/heirloom-pro/tree/main/docs/spec",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.2"
  }
}
